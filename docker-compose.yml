services:
  postgres:
    container_name: postgres-sql-cms
    image: postgres
    environment:
      POSTGRES_USER: sushil
      POSTGRES_PASSWORD: password
      PGDATA: /var/lib/postgresql/data
      POSTGRES_DB: courier_management_system
    volumes:
      - postgres:/data/postgres
    ports:
      - 5485:5432
    networks:
      - spring-demo
    restart: unless-stopped
  mail-dev:
    container_name: mail-dev-cms
    image: maildev/maildev
    ports:
      - 1080:1080
      - 1025:1025

networks:
  spring-demo:
    driver: bridge

volumes:
  postgres:
    driver: local